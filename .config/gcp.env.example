# GCP deployment target — single source of truth (gitignored as .config/gcp.env).
# Same project as IgniteAI (shared Firebase/Firestore), different Cloud Run service.
GCP_PROJECT_ID=ignite-ai-01
GCP_PROJECT_NUMBER=
GCP_ACCOUNT=you@example.com
GCP_REGION=us-central1
SERVICE_NAME=ignite-ads-backend
